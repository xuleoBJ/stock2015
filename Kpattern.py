import os
print "hello,world"
## engulfing pattern 确定趋势中的大阴包大阳，算法是第二天收盘价小于第一天的开盘价并且两根长柱子
if curStock.riseRateFList[i-1]>=2 and curStock.riseRateFList[i]<=-2 and curStock.priceCloseingFList[i]<curStock.priceOpeningFList[i-1] and  curStock.priceCloseingFList[i-1]<curStock.priceOpeningFList[i]:

## price rate	
if 0<curStock.riseRateFList[i-2]<=1 and -0.5<=curStock.riseRateFList[i-1]<=0.5 and 2<=curStock.riseRateFList[i]:
